import { Routes } from '@angular/router';

import { LoginComponent } from '@app/components/login/components/login/login.component';

export const loginRouting: Routes = [
  {
    path: 'login',
    component: LoginComponent,
  }
];
